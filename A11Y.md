# 可访问性 (A11y) 指南

本项目遵循 WCAG 2.1 AA 标准，实现了全面的可访问性支持。

## 实现的无障碍特性

### 1. 语义HTML

- 使用适当的HTML元素（`<button>`, `<textarea>`, `<article>` 等）
- 使用语义角色（`role="main"`, `role="navigation"` 等）
- 使用标记元素定义内容结构

### 2. ARIA 属性

#### 标签和描述
- `aria-label` - 为无文本元素提供标签
- `aria-labelledby` - 关联标签元素
- `aria-describedby` - 关联描述文本

#### 状态和属性
- `aria-current="page"` - 标记当前选中项
- `aria-disabled` - 指示元素是否禁用
- `aria-expanded` - 指示可展开元素的状态
- `aria-live` - 指示动态内容更新
- `aria-atomic` - 指示是否宣读所有内容

#### 表单相关
- `aria-multiline` - 指示文本框支持多行
- `aria-readonly` - 指示元素是否只读

### 3. 键盘导航

所有功能都可使用键盘访问：

- **Tab** - 在可交互元素间导航
- **Shift+Tab** - 逆向导航
- **Enter** - 激活按钮
- **Ctrl+Enter** / **Cmd+Enter** - 发送消息
- **Escape** - 关闭对话框

### 4. 表单可访问性

#### 登录页面
```html
<input
  type="text"
  id="username"
  name="username"
  autocomplete="username"
  aria-label="用户名"
/>
<input
  type="password"
  id="password"
  name="password"
  autocomplete="current-password"
  aria-label="密码"
/>
```

#### 注册页面
```html
<input
  type="text"
  id="username"
  name="username"
  autocomplete="username"
  aria-label="用户名"
/>
<input
  type="password"
  id="password"
  name="password"
  autocomplete="new-password"
  aria-label="密码"
/>
```

### 5. 颜色对比

确保所有文本和背景的对比比率至少为 4.5:1（正常文本）或 3:1（大文本）

### 6. 动态内容通知

使用 `aria-live` 区域通知屏幕阅读器：

```html
<div
  class="chat-messages"
  role="log"
  aria-live="polite"
  aria-atomic="false"
>
  <!-- 消息内容 -->
</div>
```

### 7. 焦点管理

- 清晰的焦点指示器
- 逻辑的焦点顺序
- 焦点陷阱（在模态框中）

### 8. 屏幕阅读器兼容性

- 测试过的设备：NVDA, JAWS, VoiceOver
- 动态内容使用 `aria-live="polite"`
- 状态变化使用 `role="status"`

## 测试指南

### 使用屏幕阅读器测试

#### NVDA (Windows)
1. 下载: https://www.nvaccess.org/
2. 快捷键: Ctrl+Alt+N 启动
3. Insert 是主命令键

#### JAWS (Windows)
1. 下载: https://www.freedomscientific.com/products/software/jaws/
2. 快捷键: Insert 是主命令键

#### VoiceOver (macOS/iOS)
1. 系统偏好设置 → 无障碍 → VoiceOver
2. 启用: Cmd+F5
3. 快捷键: VO = Control+Option

#### Talkback (Android)
1. 设置 → 无障碍 → Talkback
2. 快捷键: 音量键 + U

### 键盘导航测试

```bash
# 完全禁用鼠标进行测试
# 1. 仅使用Tab键导航所有交互元素
# 2. 使用Enter/Space激活按钮
# 3. 测试焦点是否明显可见
# 4. 验证焦点顺序是否逻辑
```

### 自动化测试

```bash
# 使用 axe DevTools 进行自动化检查
# 在浏览器开发工具中使用 axe 插件

# 安装 axe-core
npm install --save-dev axe-core

# 在测试中使用
import { axe, toHaveNoViolations } from 'jest-axe'
expect(toHaveNoViolations(results))
```

## 最佳实践

### 1. 提供替代文本

```html
<!-- 不好 -->
<button>></button>

<!-- 好 -->
<button aria-label="下一条消息">></button>
```

### 2. 关联标签

```html
<!-- 不好 -->
<label>用户名</label>
<input type="text" />

<!-- 好 -->
<label for="username">用户名</label>
<input type="text" id="username" />
```

### 3. 明确的焦点状态

```css
button:focus {
  outline: 2px solid #667eea;
  outline-offset: 2px;
}
```

### 4. 跳过链接

```html
<a href="#main" class="skip-link">跳过导航</a>
<main id="main">
  <!-- 主要内容 -->
</main>
```

### 5. 避免仅依赖颜色

```html
<!-- 不好：仅使用颜色区分 -->
<span class="error" style="color: red">错误</span>

<!-- 好：使用文本和颜色 -->
<span class="error" style="color: red">❌ 错误</span>
```

## 常见问题

### Q: 如何测试屏幕阅读器兼容性?
A: 下载免费的屏幕阅读器（如NVDA、Talkback）进行测试。按照上面的测试指南操作。

### Q: 如何改进键盘导航?
A: 确保所有交互元素都可通过Tab键访问，并有清晰的焦点指示器。

### Q: 为什么需要ARIA?
A: ARIA提供了屏幕阅读器所需的信息，使动态Web应用程序更易访问。

### Q: 如何测试色彩对比?
A: 使用在线工具如 WebAIM Color Contrast Checker（https://webaim.org/resources/contrastchecker/）

## 资源

- [WCAG 2.1 指南](https://www.w3.org/WAI/WCAG21/quickref/)
- [ARIA 实践指南](https://www.w3.org/WAI/ARIA/apg/)
- [WebAIM 文章](https://webaim.org/)
- [MDN Web Docs - A11y](https://developer.mozilla.org/zh-CN/docs/Learn/Accessibility)
- [Vue 3 可访问性](https://v3.vuejs.org/guide/a11y-basics.html)

## 更新日志

### v1.0.0
- ✅ 实现基础ARIA标签和角色
- ✅ 支持键盘导航
- ✅ 实现动态内容通知
- ✅ 添加表单可访问性
- ✅ 测试屏幕阅读器兼容性
