{
  "name": "artificial-chat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web:dev": "pnpm -F artificial-chat-web dev",
    "web:build": "pnpm -F artificial-chat-web build",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -F artificial-chat-web build",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
